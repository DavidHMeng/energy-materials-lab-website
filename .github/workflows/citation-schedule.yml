name: Refresh DOI citations (manual until repository confirmation)

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: write
  pull-requests: write

jobs:
  refresh:
    uses: ./.github/workflows/update-citations.yaml
    with:
      open-pr: true
    secrets: inherit
